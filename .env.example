# ─────────────────────────────────────────────────────────────────────────────
# Use the file that matches your deployment target:
#
#   .env.local.example    Local desktop  — Ollama + langgraph dev --tunnel
#   .env.vastai.example   Vast.ai        — Ollama + startup.sh (auto-generated)
#   .env.railway.example  Railway        — OpenRouter API + Docker container
#   .env.client.example   Browser        — Transformers.js (no backend server)
#
# Copy the relevant file to .env:
#   cp .env.local.example .env
# ─────────────────────────────────────────────────────────────────────────────
